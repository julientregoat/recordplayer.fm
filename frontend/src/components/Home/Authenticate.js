import React from 'react';

import { Grid } from 'semantic-ui-react'

const Authenticate = ({}) => (
  <React.Fragment>
    <Grid.Row><p> Thanks for joining recordplayer.</p> </Grid.Row>
  </React.Fragment>
);

export default Authenticate;
